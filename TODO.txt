add categories modification on profile and show it on them
add rating system to all profiles
add confirmation email in registration
add active inactive looking for job
add language select basic example JSON 
add backend call to add credit and subtract credit
add user searchable when paid
add front end add view with payment history, balance and payment button
add mongodb to CRUD with setting configuration There should be a parameter in the settings config file we can change to swith this on and off. There should be a base CRUD class / model /extended that now will have two derived classes now one nodejs and mongodb. 
